<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:beans="http://www.springframework.org/schema/c"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc
        https://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/context
        https://www.springframework.org/schema/context/spring-context.xsd">




    <!-- 어노테이션 기반으로 동작하도록 설정 추가 -->
    <mvc:annotation-driven/>



    <!-- msimpl에 @Autowired 필수-->
    <!-- base-package의 패키지이름을 기준으로 스캔, 빈 자동생성, 애노테이션-와이어링 자동 연결한다.
 RestController 사용을 위해 서블릿xml을 하나 더 작성했고
 컨트롤러 경로가 다르기 때문에 공통 폴더를 작성하면 얘네가 골라서 갈 수 없으므로
 각 서블릿.xml에 경로를 작성해준다.




https://m.blog.naver.com/PostView.nhn?blogId=todoskr&logNo=220845006916&proxyReferer=https:%2F%2Fwww.google.com%2F
  서블릿 .xml 파일이 두개 이상이면
  default handler인 base-package(어노테이션을 이용해 url과 controller 연결 )보단  다른 handler를 사용해야될듯..


-->

    <context:component-scan base-package="com.company.souvcoffee.MS"/>




    <!-- 정적소스
    webapp
        /resources
           /css
           /font
           /js
    dispater-servlet에서 / 로 다 받는데  /resources/** 로 요청이오면  /resources/로 매핑
    경로에 대한 alias같은거라고 생각 / /resources/**오면 /resources/로 사용

    /resources/** 로 들어오는 모든 요청은 ${webapproot}/resources/ 로 모두 매핑시켜버리겠다는 설정

    -->
    <mvc:resources mapping="/resources/**" location="/resources/"/>

    <!-- View Resolver 설정, views 폴더 생성해야된다. -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>



</beans>