<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc
       http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!--!  ! RestController servlet !!!!-->

    <!-- This tag registers the DefaultAnnotationHandlerMapping and
         AnnotationMethodHandlerAdapter beans that are required for Spring MVC  -->
    <mvc:annotation-driven />
    <!-- This tag allows for mapping the DispatcherServlet to "/" -->
    <mvc:default-servlet-handler />

    <!-- Process annotations on registered beans like @Autowired... -->
    <context:annotation-config/>


    <!-- msimpl에 @Autowired 필수-->
    <!-- 컴포넌트 스캔 , 스캔 순서 는 dao - service - controller.Rest
    패키지를...다시 짜야될듯 정신없음-->
    <context:component-scan base-package="com.company.souvcoffee.dao.msimpl"/>
    <context:component-scan base-package="com.company.souvcoffee.service.msimpl"/>
    <context:component-scan base-package="com.company.souvcoffee.controller.rest" />


    <mvc:resources mapping="/resources/**" location="/resources/"/>



</beans>